#ifndef DT_LECCION
#define DT_LECCION

/* ---------------------------------------------- Includes ---------------------------------------------- */
#include "../Utils.hh"
/* ------------------------------------------------------------------------------------------------------ */

class DTLeccion
{
private:
    string Tema;
    string Objetivo;
public:
    // Constructores
    DTLeccion(string tema, string obj);
    // Destructor
    ~DTLeccion();
    // Getters ?
    
};
#endif